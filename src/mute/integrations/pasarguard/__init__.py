"""PasarGuard integration: API client, models, and feature modules.

``DISPLAY_NAME`` is the human-facing name of this integration. The application reads it so
the UI is never hardcoded to a specific panel — future integrations expose their own name.
"""

DISPLAY_NAME = "PasarGuard"
